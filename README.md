# Path of Exile Loot Filter Editor
Simple editor for editing path of exile loot filters.

Supports some unique features:
 * Disable (and re-enable) blocks
 * Item preview
 * Section support
 
Features in plan:
 * BaseType & Class database
